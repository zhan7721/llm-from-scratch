[pytest]
# Collect tests.py in addition to default test_* pattern
python_files = tests.py test_*.py
# Use importlib mode to avoid module name collisions across chapter dirs
addopts = --import-mode=importlib
